# Dump PE's embedded inside other PE's

This repo was made to find PE's embedded inside other PE's due to a specific research where a binary had 2 other binaries embedded inside itself
To use, simply drag the binary that you wish to check for embedded PE's on top of it

Tested on Windows and made for Windows
